services:
  gerenciamento-front:
    image: gerenciamento-front
    container_name: gerenciamento-front
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "4000:4000"
